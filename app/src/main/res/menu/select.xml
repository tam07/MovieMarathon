<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/select_menu"
        android:orderInCategory="1"
        android:showAsAction="ifRoom" 
        android:icon="@drawable/ic_refresh"
        android:onClick="launchRefresh"/>

</menu>
